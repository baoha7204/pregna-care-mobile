export enum UserStatus {
  Registered = "registered",
  Active = "active",
  Banned = "banned",
}

export enum UserRoles {
  Admin = "admin",
  Member = "member",
}

export enum FetusGender {
  Male = "male",
  Female = "female",
  Unknown = "unknown",
}
